# Environment
.env

# Composer dependencies
vendor/

# Uploads (user-submitted files)
public/uploads/*
!public/uploads/.gitkeep

# Docker volumes / overrides
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Claude Code — local machine settings (permissions, allowlists)
.claude/settings.local.json

# Claude Code — conversation memory (machine-local, never commit)
memory/

# Cloudflare tunnel secrets and active machine config (machine-specific, never commit)
cloudflared/creds.json
cloudflared/config.yml
cloudflared/dev-creds.json
cloudflared/prod-creds.json

# Working notes (local only)
POLISH.md
QA_PLAN.md
changes.md
changes001.md
DIAGRAMS.md
setup.md
NOTICE.md
USER_MANUAL.md
context.md

.claude
CLAUDE.md

